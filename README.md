# voxels

```sudo apt update
sudo apt install build-essential cmake libglfw3-dev libgl1-mesa-dev libglu1-mesa-dev xorg-dev```


- every class or class pointer is PascalCase
    e.g. Shader* VertexShader
- every getter is snake_case